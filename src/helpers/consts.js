export const ROUTES = {
  home: '/valantis-test-task/',
}

export const isDevEnv = import.meta.env.DEV;

export const API_PASSWORD = import.meta.env.VITE_API_PASSWORD;

export const LIMIT_PRODUCTS = 50
